To run against local dev, you can use the cypress app normally, or use the run-in-docker.sh script. 

The docker script will also pass any args to cypress so you can do stuff like run-in-docker.sh --spec cypress/integration/login.spec.js (would run just one spec).

name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: clj-kondo checks
      uses: DeLaGuardo/clojure-lint-action@v1
      with:
        clj-kondo-args: --lint app/backend/src
